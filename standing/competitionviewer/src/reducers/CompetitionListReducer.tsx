import { ICompetitionListItem } from '../state/ICompetitionListItem';

export const competitionListReducer = (state: ICompetitionListItem[] = [], action: {}): ICompetitionListItem[] => {
    return state;
}
